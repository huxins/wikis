# 箭头函数

```javascript
let func = (arg1, arg2, ...argN) => expression;
let sum = (a, b) => a + b;
let double = n => n * 2;
let sayHi = () => alert("Hello!");
```

多行的箭头函数

```javascript
let sum = (a, b) => {
  let result = a + b;
  return result;
};
```

